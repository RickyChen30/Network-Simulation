# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Local env / editor / tooling
*.local
.env
.env.*
.DS_Store
.claude/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite / TS caches
.vite/
*.tsbuildinfo

# C core build artifacts — keep the source (native/*.c/*.h/Makefile), never the
# compiled binaries. Rebuild with `make wasm` (browser) / `make test` (native).
*.wasm
native/tcp_test
